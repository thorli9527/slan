name: slan_client_v2
description: Thin Flutter shell for SLAN client v2.
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  client_core_plugin:
    path: ../plugins/client_core_plugin

dev_dependencies:
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
